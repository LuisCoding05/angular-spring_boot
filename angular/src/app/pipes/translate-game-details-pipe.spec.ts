import { TranslateGameDetailsPipe } from './translate-game-details-pipe';

describe('TranslateGameDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateGameDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
